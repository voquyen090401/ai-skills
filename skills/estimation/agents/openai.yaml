interface:
  display_name: "estimation"
  short_description: "ERP breakdown at just-enough estimation granularity"
  default_prompt: "Use $estimation to analyze the provided ERP documents and produce an evidence-based Big/Medium/Small task breakdown in Vietnamese at just-enough business granularity, not micro coding granularity, and output the result in the mandatory table with ProgramID, 機能区分, 機能名, Big, Medium, Small, Note, Type, and Complexity."

policy:
  allow_implicit_invocation: true
