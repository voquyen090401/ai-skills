interface:
  display_name: "qa"
  short_description: "Customer-ready BrSE QA with explicit business understanding and evidence-bound confirmation points"
  default_prompt: "Use $qa to draft Vietnamese BrSE clarification QA from the provided evidence. Follow the QA skill instructions and reference files. Preserve exact screen, sheet, line, item, and Japanese business terms when available. Return only the final Vietnamese customer message unless the user explicitly asks for dataset output."

policy:
  allow_implicit_invocation: true
