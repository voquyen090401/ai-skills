interface:
  display_name: "qa"
  short_description: "BrSE investigation QA for ERP clarification"
  default_prompt: "Use $qa to draft BrSE investigation QA from the attached spec, gold dataset, and current-system evidence, keeping the output grounded in investigation and structured with opening, body, and closing."

policy:
  allow_implicit_invocation: true
